# [005] 测试 fixture + 端到端测试 + 新 SKILL.md

## 📌 Spec 引用
来源：[`.workflow/specs/json-to-ppt-skill.md`](../specs/json-to-ppt-skill.md) 的「🧪 测试决策」「解决方案 / 调用方式」

## 🎯 任务
在仓库根 `tests/` 下新增 `tests/test_json_to_ppt.py` 和 `tests/fixtures/json-to-ppt/`，包含四个 JSON fixture（纯文字、多元素组合、表格、含模板路径）以及对应的 pytest 端到端测试。同时新建 `skills/json-to-ppt/SKILL.md` 真实内容（覆盖 placeholder）。

现在 `tests/test_editable_pptx_skill.py` 只有两个 case；新 skill 没有自己的测试或 SKILL.md。任务完成后：

- 4 个 fixture 文件存在于 `tests/fixtures/json-to-ppt/`
- `pytest tests/test_json_to_ppt.py -q` 全部通过
- 每个 fixture 至少 1 个测试断言：PPTX 可打开、slide 数符合、关键文字可读
- `skills/json-to-ppt/SKILL.md` 是真实 SKILL.md（有 YAML frontmatter name + description），不再 placeholder

## ✅ 验收标准
1. `ls tests/fixtures/json-to-ppt/` 列出 4 个 `.json` 文件
2. `pytest tests/test_json_to_ppt.py -q` 退出码 0
3. SKILL.md 首行是 `---`，包含 `name: json-to-ppt` 和 `description:` 字段
4. SKILL.md 的「Hard Requirements」段至少 3 条 bullet
5. SKILL.md 的「Portable Renderer」段含 `python scripts/build_pptx.py --spec ... --out ...` 调用示例

## 🧪 测试 seam
- pytest 端到端：`tests/test_json_to_ppt.py`
- shell：`ls`、`pytest`、`head skills/json-to-ppt/SKILL.md`

## 🚧 阻塞
- 依赖：[001] scaffold（目录要先有）
- 依赖：[003] `build_pptx.py` CLI（fixture 测试要调它）
- 依赖：[002] warning + continue（fixture 含坏元素的测试依赖新行为）
- 不依赖 [004] spec-format 扩展（fixture 只用旧 spec 子集）

## 🤖 建议 Agent
Claude
理由：测试覆盖 + SKILL.md 写作属于 `core-domain`

## 🌿 分支
`ticket/005-test-fixtures-and-skill-md`

## 🧭 上下文
- 现有测试文件：[`tests/test_editable_pptx_skill.py`](../../tests/test_editable_pptx_skill.py) 用 `importlib.util.spec_from_file_location` 动态加载脚本——新测试**用相同模式**加载 `skills/json-to-ppt/scripts/build_pptx.py`
- 旧 SKILL.md：[`skills/images-to-editable-pptx/SKILL.md`](../../skills/images-to-editable-pptx/SKILL.md) 是范本——frontmatter `name` + `description`、Hard Requirements、Workflow、Reconstruction Rules、Portable Renderer、Delivery Gate
- 4 个 fixture 内容建议：
  - `text-only.json`：1 张 slide，1 个 text 元素
  - `mixed-elements.json`：1 张 slide，含 text + shape + line 各 1
  - `table.json`：1 张 slide，含 table 元素（rows 至少 3×3）
  - `with-template.json`：spec 引用 `--template` 路径的 PPTX（用 `tests/fixtures/sample-template.pptx`，由 ticket 现场生成）
- SKILL.md 的 description 要写清触发场景："Use when Codex or another agent must create an editable .pptx from a structured JSON spec (e.g. generated by an LLM) without going through image generation."
- preflight 命令：`python -m pytest`（来自 `config.json`）